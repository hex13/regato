{
  "name": "regato",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx esbuild --bundle src/index.tsx  --outdir=public --servedir=public",
    "ts": "npx tsc --project .  --noemit"
  },
  "author": "= <=>",
  "license": "UNLICENSED",
  "dependencies": {
    "esbuild": "^0.16.17",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.26",
    "@types/react-dom": "^18.0.10",
    "typescript": "^4.9.4"
  }
}
