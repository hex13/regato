{
  "name": "regato",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx esbuild --bundle src/index.ts  --outdir=public --servedir=public"
  },
  "author": "= <=>",
  "license": "UNLICENSED",
  "dependencies": {
    "esbuild": "^0.16.17"
  }
}
